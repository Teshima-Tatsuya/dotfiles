# ~/.tmuxinator/myenv.yml

name: myenv
root: /vagrant_data/newwiki
tmux_options: -2

windows:
  - editor: vim
  - git:
  - dotfile:
    - cd ~/dotfiles
    - vim
  - server: bundle exec rails s
  - guard: bundle exec guard -p
